[project]
name = "jobproof-api"
version = "0.1.0"
description = "FastAPI backend for JobProof's explainable career intelligence engine."
requires-python = ">=3.13"
dependencies = [
  "fastapi==0.116.2",
  "uvicorn[standard]==0.35.0",
  "pydantic==2.11.9",
]

[tool.uv]
package = false
